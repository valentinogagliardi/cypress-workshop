context("Application", () => {
  describe("Authentication", () => {
    it("[Insert persona here] can login", () => {
      /**
       * Your turn here ... we need to:
       * 1. create a custom command for login
       * 2. testing all the login/logout flow!
       */
      // Your assertions here
    });

    it("[Insert persona here] should redirect to login page if cookie expires", () => {
      // Your assertions here
    });

    it("[Insert persona here] should redirect to index after login", () => {
      // Your assertions here
    });

    it("[Insert persona here] cannot login with invalid credentials", () => {
      // Your assertions here
    });

    it("[Insert persona here] can logout", () => {
      // Your assertions here
    });
  });
});
