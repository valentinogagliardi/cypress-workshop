# Cypress Workshop quickstart

Installing dependencies:

```shell
npm ci
```

Running the project:

```shell
npm run dev
```

Running e2e tests:

```shell
npm run dev
```

Then in another shell:

```shell
npm run e2e
```
